<div ng-controller="NavCtrl">
   <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand" href="/">CommonPlaylist</a>
      </div>
      <ul class="nav navbar-nav navbar-right">
        <li ng-hide="signedIn()"><a href="/login">Sign In</a></li>
        <li ng-hide="signedIn()"><a href="/register">Sign Up</a></li> 
        <li ng-show="signedIn()"><a href="/">{{user.username}}</a></li> 
        <li ng-show="signedIn()"><a ng-click="logout()">Sign Out</a></li> 
      </ul>
    </div>
  </nav>
</div>